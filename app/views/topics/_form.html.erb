<%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= f.text_field :topic, class: "input-block-level", placeholder: "Topic Name"%>
    <%= f.number_field :subject_id, class: "input-block-level", placeholder: "Subject" %>
    <%= f.text_area :description, class: "editor input-block-level", :rows => '10', placeholder: "Description" %>
    <hr>
    <%= f.submit "Create", id: "submit", class: "btn btn-block"%>
<% end %>